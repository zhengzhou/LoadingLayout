<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.zayn.swiprefresh">

    <application
        android:name=".DemoApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity"></activity>
        <activity
            android:name=".SimpleLoadingActivity"
            android:label="simpleLoading"></activity>
        <activity
            android:name=".CustomNestedLoadingActivity"
            android:label="NestedLoading"></activity>
        <activity
            android:name=".NestedLoadingActivity"
            android:label="NestedLoading">

        </activity>
        <activity android:name=".NestedHorizontalLoadingActivity" />
        <activity
            android:name=".ScrollingActivity"
            android:label="@string/title_activity_scrolling"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>