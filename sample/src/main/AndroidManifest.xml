<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.zayn.swiprefresh">

    <application
        android:name=".DemoApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SimpleLoadingActivity" />
        <activity
            android:name=".CustomLoadingActivity"
            android:label="simpleLoading" />
        <activity
            android:name=".CustomRefreshActivity"
            android:label="NestedLoading">
        </activity>
        <activity
            android:name=".SimpleRefreshActivity"
            android:label="NestedLoading"></activity>
        <activity android:name=".NestedHorizontalLoadingActivity" />
        <activity
            android:name=".NestedParentActivity"
            android:label="@string/title_activity_scrolling"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity
            android:name=".FirstActivity"
            android:label="@string/title_activity_first"
            android:theme="@style/AppTheme.NoActionBar">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>